{{{Aside}}}

<section class='profile-section'>
  {{{Avatar}}}

  <form action='#'>
    <div class='profile-info'>

      {{#each editDataFields}}
        {{{this}}}
      {{/each}}

      <div class='buttons-container'>
        {{{EditDataButton}}}
      </div>
    </div>
  </form>
</section>

{{#if showModal}}
  {{{Modal}}}
{{/if}}
